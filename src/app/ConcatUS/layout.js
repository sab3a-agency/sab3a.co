export const metadata = {
    title: 'تواصل معنا',
    description: 'تواصل معنا',
}
export default function RootLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
};